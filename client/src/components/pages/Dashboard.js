import React from 'react';
import PropTypes from 'prop-types';

const Dashboard = (props) => {
  return <div>Ceci est le Dashboard des users</div>;
};

Dashboard.propTypes = {};

export default Dashboard;
